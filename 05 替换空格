class solution
{
public:
    string replaceSpace(string s)
    {
        sting s;
        for(auto c : s)//遍历字符串
        {
            if(c==' ')//遇到空格就替换为%20
              res+="%20";
            else
              res+=c;
        }
        return res;
    }
}
